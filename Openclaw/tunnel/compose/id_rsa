put your private key here

make sure to set permissions to 600 (chmod 600 id_rsa)

generate a new key with `ssh-keygen -t ed25519 -f ./id_rsa` if you don't have one yet, and add the public key to your server's `~/.ssh/authorized_keys` file.
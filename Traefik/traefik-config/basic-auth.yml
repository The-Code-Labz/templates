http:
  middlewares:
    no-auth:
      IPAllowList:
        sourceRange:
          - "192.168.1.100/32"  # First specific IP
          - "192.168.1.101/32"  # Second specific IP
tcp:
  middlewares:
    no-auth:
      IPAllowList:
        sourceRange:
          - "192.168.1.100/32"  # First specific IP
          - "192.168.1.101/32"  # Second specific IP          